package com.ims.database;

public class DBProducts {
    public enum Column {
        ID,
        NAME,
        CATEGORY_ID,
        CURRENT_STOCKS,
        EXPECTED_STOCKS,
        LAST_MODIFIED
    }
}
